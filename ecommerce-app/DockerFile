# Base image for PHP
FROM php:8.4-cli

# Install dependencies for Laravel and Node.js for Vite
RUN apt-get update -y && apt-get install -y libzip-dev zip unzip git nodejs npm

# Install required PHP extensions for database
RUN docker-php-ext-install pdo_mysql zip

# Install Composer
COPY --from=composer:latest /usr/bin/composer /usr/bin/composer

# Set working directory
WORKDIR /app

# Copy all project files to the container
COPY . .

# Install PHP and Node dependencies
RUN composer install --no-dev --optimize-autoloader
RUN npm install && npm run build

# Start the Laravel server
CMD bash -c "php artisan serve --host=0.0.0.0 --port=${PORT:-10000}"

# Install dependencies 
RUN apt-get update -y && apt-get install -y libpq-dev libzip-dev zip unzip git nodejs npm

# Install required PHP extensions 
RUN docker-php-ext-install pdo_mysql pdo_pgsql zip